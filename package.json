{
  "name": "personal-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Alex Ramos — personal portfolio site.",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "start": "astro dev --host 0.0.0.0",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  }
}
